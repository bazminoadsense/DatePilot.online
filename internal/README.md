# Internal Project Documentation

This directory contains DatePilot planning and operational documentation for staff. It is version-controlled for the team but is not public website content. Deployment must publish only application build output and approved public assets; it must not serve `internal/`.
